[package]
name = "overlay-core"
version = "0.1.0"
edition = "2021"
description = "Pure coordinate/tracking logic for The Isle overlay. No I/O, no OS calls."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
