[package]
name = "localpos"
version = "0.1.0"
edition = "2021"
description = "Pure decoder + lock tracker for The Isle client movement packets. No I/O, no OS calls, no capture."

# Faithful port of IsleLiveMap's TheIsleOverlay.LocalTelemetry (MIT,
# klong-dev/IsleLiveMap 1.3.1): UnrealMovementPacketDecoder, LocalMovementTracker
# and the heading helpers. The Npcap capture loop and UDP-port lookup live in
# the tauri crate (src/localpos/), same split as overlay-core vs clipboard.rs.

[dependencies]
